---
import { altPath, localePath, useTranslations, type Locale } from '../i18n';

interface Props {
  showLogo?: boolean;
  locale?: Locale;
}

const { showLogo = true, locale = 'en' } = Astro.props;
const t = useTranslations(locale);
const alternate = altPath(Astro.url.pathname);

const navLinks = [
  { href: localePath(locale, '/work'), label: t('nav.work') },
  { href: localePath(locale, '/blog'), label: t('nav.journal') },
  { href: localePath(locale, '/about'), label: t('nav.about') },
  { href: localePath(locale, '/contact'), label: t('nav.contact') },
];
---

<header
  id="site-header"
  class="fixed top-0 left-0 w-full z-50 transition-transform duration-500 ease-editorial"
>
  <div class="bg-[color:var(--bg)]/85 backdrop-blur-md border-b rule-line">
    <nav class="container-custom py-4 flex items-center justify-between gap-6">
      {
        showLogo ? (
          <a
            href={localePath(locale, '/')}
            class="logo font-mono text-[11px] font-semibold tracking-[0.18em] uppercase text-[color:var(--text)]"
          >
            marcin skalski
          </a>
        ) : (
          <span aria-hidden="true" />
        )
      }

      <button
        id="mobile-menu-toggle"
        class="desktop:hidden flex flex-col gap-1 w-11 h-11 justify-center items-center p-2 rounded hover:opacity-70 focus:outline-none"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <span class="w-6 h-px bg-[color:var(--text)] transition-all"></span>
        <span class="w-6 h-px bg-[color:var(--text)] transition-all"></span>
        <span class="w-6 h-px bg-[color:var(--text)] transition-all"></span>
      </button>

      <div
        id="mobile-menu"
        class="hidden desktop:flex desktop:items-center desktop:gap-10 absolute desktop:relative top-full left-0 w-full desktop:w-auto bg-[color:var(--bg)] desktop:bg-transparent p-6 desktop:p-0 border-b rule-line desktop:border-0"
      >
        <ul
          class="flex flex-col desktop:flex-row gap-6 desktop:gap-8 text-[12px] tracking-[0.14em] uppercase font-mono"
        >
          {
            navLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-[color:var(--text)] hover:opacity-70 transition-opacity"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>

        <div class="flex items-center gap-5 mt-6 desktop:mt-0">
          <a
            href={alternate.href}
            class="font-mono text-[11px] tracking-[0.14em] uppercase text-[color:var(--text)] hover:opacity-70 transition-opacity"
            aria-label={alternate.locale === 'pl' ? 'Wersja polska' : 'English version'}
          >
            {alternate.locale}
          </a>
          <button
            id="theme-toggle"
            type="button"
            class="text-[color:var(--text)] hover:opacity-70 transition-opacity"
            aria-label="Toggle dark mode"
          >
            <svg
              class="w-[18px] h-[18px] block dark:hidden"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M21.752 15.002A9.718 9.718 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z"
              ></path>
            </svg>
            <svg
              class="w-[18px] h-[18px] hidden dark:block"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z"
              ></path>
            </svg>
          </button>
        </div>
      </div>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');

  toggle?.addEventListener('click', () => {
    const isHidden = menu?.classList.toggle('hidden');
    toggle.setAttribute('aria-expanded', isHidden ? 'false' : 'true');
  });

  document.addEventListener('click', (e) => {
    if (!toggle?.contains(e.target as Node) && !menu?.contains(e.target as Node)) {
      menu?.classList.add('hidden');
      toggle?.setAttribute('aria-expanded', 'false');
    }
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !menu?.classList.contains('hidden')) {
      menu?.classList.add('hidden');
      toggle?.setAttribute('aria-expanded', 'false');
      toggle?.focus();
    }
  });

  // Hide-on-scroll header
  const header = document.getElementById('site-header');
  if (header) {
    let lastY = window.scrollY;
    let ticking = false;
    const update = () => {
      const y = window.scrollY;
      const goingDown = y > lastY;
      if (y > 80 && goingDown) {
        header.style.transform = 'translateY(-100%)';
      } else {
        header.style.transform = '';
      }
      lastY = y;
      ticking = false;
    };
    window.addEventListener(
      'scroll',
      () => {
        if (!ticking) {
          window.requestAnimationFrame(update);
          ticking = true;
        }
      },
      { passive: true }
    );
  }

  // Theme toggle
  const themeToggle = document.getElementById('theme-toggle');
  themeToggle?.addEventListener('click', () => {
    const root = document.documentElement;
    const next = root.classList.toggle('dark') ? 'dark' : 'light';
    try {
      localStorage.setItem('theme', next);
    } catch {
      /* ignore storage errors */
    }
  });
</script>
